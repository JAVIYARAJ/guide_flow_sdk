<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F8FAFC">

    <include layout="@layout/layout_standard_app_bar" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="24dp">
        
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Progress Indicators"
                android:textSize="22sp"
                android:textStyle="bold"
                android:textColor="#0F172A"
                android:layout_marginBottom="16dp" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Tour configurations can optionally show progress to users. Try the different styles below."
                android:textSize="16sp"
                android:textColor="#475569"
                android:layout_marginBottom="32dp" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnDots"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:text="Show DOTS Indicator"
                android:textSize="16sp"
                app:cornerRadius="16dp"
                android:layout_marginBottom="16dp"
                android:backgroundTint="#8B5CF6" />

            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnText"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:text="Show TEXT Indicator"
                android:textSize="16sp"
                app:cornerRadius="16dp"
                android:layout_marginBottom="16dp"
                android:backgroundTint="#F59E0B" />
                
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnNone"
                android:layout_width="match_parent"
                android:layout_height="64dp"
                android:text="NO Indicator"
                android:textSize="16sp"
                app:cornerRadius="16dp"
                android:backgroundTint="#94A3B8" />

        </LinearLayout>
    </ScrollView>
</LinearLayout>
