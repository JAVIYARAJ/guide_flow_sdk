<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GuideFlow">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name=".ActivityDemo" />
        <activity android:name=".FragmentDemoActivity" />
        <activity android:name=".ListDemoActivity" />
        <activity android:name=".SpotlightDemoActivity" />
        <activity android:name=".ErrorHandlingDemoActivity" />
        <activity android:name=".NavigationDemoActivity" />
        <activity android:name=".SmartTourDemoActivity" />
        <activity android:name=".JsonTourDemoActivity" />
        <activity android:name=".CustomViewDemoActivity" />
        <activity android:name=".GlassmorphismDemoActivity" />
        <activity android:name=".StepIndicatorDemoActivity" />
    </application>
</manifest>